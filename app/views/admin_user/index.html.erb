<body class="Backgruond">
<% case current_user.tipo %>
<% when 1 %>
    <% @usuario = "Administrador" %>
<% when 2 %>
    <% @usuario = "Control Académico" %>
<% when 3 %>
    <% @usuario = "Extraescolar" %>
<% when 4 %>
    <% @usuario = "Biblioteca" %>
<% end %>
<h1 class ="text-center">Bienvenido, ha ingresado como: <%= @usuario %></h1>
<div class="container text-center">
    <form class="text-center" action="">
        <div class="text-justify mb-4">

        <h3 class="text-center">¿Qué es un ETL?</h3>
        <h3 class="Definition_etl">
            Los procesos ETL son un término estándar que se utiliza para referirse al movimiento y transformación de datos, este termino viene de ingles de las siglas Extract-Transform-Load que significan Extraer, Transformar y Cargar y se refiere a los datos en una empresa
            Consiste en un proceso que permite a las organizaciones mover datos desde múltiples fuentes, reformatearlos y cargarlos 
            en otra base de datos (denominada data mart o data warehouse) con el objeto de analizarlos. 
            Los datos también pueden ser enviados a otro sistema operacional para apoyar un proceso de negocio.
        </h3>
        </div>
        <div class="text-center mb-4">
            <%= image_tag("ETL.jpg", size: "250*250") %>
        </div>
    </form>
</div>
<br>
<% if !@empty %>
    <div class="text-center">
        <%= link_to "Continuar editando datos", "/show_tables", method: :get , class: 'btn btn-primary btn-lg'%>
    </div>
<% end %>
<% if current_user.tipo != 1 && @empty %>
    <div class="text-center">
        <h3 class ="alert alert-danger">¡El ETL sólo lo puede generar el Administrador!</h3>
    </div>
<% end %>
<% if current_user.tipo == 1 %>
     <div class="text-center">
        <%= link_to "Generar ETL", etl_path, method: :get , class: 'btn btn-primary btn-lg'%>
    </div>
<% end %>
</body>